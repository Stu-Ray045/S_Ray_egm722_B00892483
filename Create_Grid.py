import geopandas as gpd # Import the geopandas module
from shapely import geometry # Import the geometry function from the shapely module

# determine grid parameters
set_crs(EPSG:3857) # Set the project crs
